# =============================================================================
# osm-india-etl — runtime dependencies (pip fallback)
#
# PREFERRED: this project is managed with uv + pyproject.toml:
#     uv sync                  # create .venv and install everything
#     uv pip install -e .      # editable install into an active venv
#
# This file is a pip-installable mirror of [project.dependencies] in
# pyproject.toml, provided as a fallback for environments without uv:
#     pip install -r requirements.txt
#
# Keep it in sync with pyproject.toml when dependencies change.
# =============================================================================

# HTTP / async download
requests>=2.32
httpx>=0.27
aiohttp>=3.10
aiofiles>=24.1

# OSM parsing (pyosmium)
osmium>=3.7

# Dataframes / columnar
polars>=1.9
pandas>=2.2
pyarrow>=17.0
duckdb>=1.1

# Geospatial
geopandas>=1.0
shapely>=2.0

# Databases
sqlalchemy>=2.0
psycopg[binary]>=3.2
geoalchemy2>=0.15

# Search / fuzzy / phonetic
rapidfuzz>=3.10
jellyfish>=1.1
unidecode>=1.3
python-slugify>=8.0

# API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# CLI / logging / config
typer>=0.12
rich>=13.9
loguru>=0.7
pyyaml>=6.0
psutil>=6.1
tenacity>=9.0
